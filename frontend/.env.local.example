# Copy this file to .env.local and fill in real values.
# .env.local is gitignored.

# Backend API base URL (Role B's FastAPI deploy).
# If left empty, the frontend uses a built-in mock so you can develop without the backend.
# Local backend example:    http://localhost:8000
# Render deploy example:    https://asha-ai-backend.onrender.com
NEXT_PUBLIC_API_BASE=
